name: Build APK
on:
  push:
    branches: [main]
    # Web-only changes (web/**) and CI/doc tweaks don't need an APK build.
    # Vercel still deploys web on every push regardless of this filter.
    paths-ignore:
      - 'web/**'
      - '.github/**'
      - '**.md'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Fix app.json
        run: node scripts/fix-app-json.js

      - name: Prebuild Android
        run: npx expo prebuild --platform android --clean
        env:
          EXPO_PUBLIC_API_URL: https://ireader.onrender.com

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: iReader-Pro-APK
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 7
